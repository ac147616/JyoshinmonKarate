@model JyoshinmonKarate.Models.Schedule

@{
    ViewData["Title"] = "Class Schedule Details";

    string instructorName = Model.Instructor.User.FirstName + " " + Model.Instructor.User.LastName;
}

<section class="app-form-page">
    <div class="app-form-card">

        <div class="app-form-header">
            <h1>Class Schedule Info</h1>
        </div>

        <div class="details-grid">

            <div class="details-item">
                <span>Dojo</span>
                <p>@Model.Club.ClubName</p>
            </div>

            <div class="details-item">
                <span>Instructor</span>
                <p>@instructorName</p>
            </div>

            <div class="details-item">
                <span>Level</span>
                <p>@Model.Level</p>
            </div>

            <div class="details-item">
                <span>Day of Week</span>
                <p>@Model.DayOfWeek</p>
            </div>

            <div class="details-item">
                <span>Start Time</span>
                <p>@Model.StartTime.ToString("hh:mm tt")</p>
            </div>

            <div class="details-item">
                <span>End Time</span>
                <p>@Model.EndTime.ToString("hh:mm tt")</p>
            </div>

        </div>

        <div class="form-actions">
            <a asp-action="Edit" asp-route-id="@Model.ScheduleId" class="app-primary-btn">Edit Schedule</a>
            <a asp-action="Index" class="app-cancel-link">Back to List</a>
        </div>

    </div>
</section>